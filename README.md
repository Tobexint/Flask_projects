Setting up a backend server with flask(WSGI),
routing, debugging, working with the requests
module(for HTTP methods), rendering templates, 
and many more.

USAGE:
$ export FLASK_APP='filename'
$ flask run
